// Write a program to print the multiplication table of a given number using a loop.

let a = 2; // replace the number as required
let product = 0;

for (i = 0; i < 10; i++) {
  product = product + a;
  console.log(`${product}`);
}
